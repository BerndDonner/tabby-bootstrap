# ---------------------------------------------
# 🧩 Bootstrap repository: Ignore local + secret files
# ---------------------------------------------

# ---------------------------------------------------------------------
# Generated local seed (never commit)
# ---------------------------------------------------------------------
# The redacted template is tracked:
#   secrets/seed_stripped.py
# The injected local file is intentionally untracked:
secrets/seed.py

# 🧠 Decrypted environment files (never commit)
.env
.env.*
*.key
id_*

# ✅ Only encrypted files may be tracked
!secrets/*.enc

# 🔐 SSH agent sockets or temporary decrypted data
/tmp/*
*.tmp
*.bak
*.swp

# ---------------------------------------------
# 🧰 System / editor noise
# ---------------------------------------------
*.log
.DS_Store
Thumbs.db

# VS Code
.vscode/
*.code-workspace

# JetBrains / PyCharm
.idea/

# Python cache
__pycache__/
*.pyc

# Shell history or local config
.bash_history
.gitconfig.local

